# Bowser local environment (copy to .env — never commit .env)
# Required for Claude Code agent demos / hop workflows:
ANTHROPIC_API_KEY=

# Optional — MiniMax or other Anthropic-compatible gateway
# ANTHROPIC_BASE_URL=
# ANTHROPIC_AUTH_TOKEN=
# MINIMAX_API_KEY=

# Optional — third-party tools used by archived one-offs only
# KIE_API_KEY=
