stories:
  - name: "Dashboard loads with charts and data"
    url: "http://127.0.0.1:5500/03-analytics-dashboard/"
    workflow: |
      Navigate to http://127.0.0.1:5500/03-analytics-dashboard/
      Verify the dashboard loads with stat cards showing numbers
      Verify at least one chart or data visualization is visible
      If a time-range selector exists (e.g. 30d), click it and verify the UI responds
      Verify a recent activity table or list is visible when present
